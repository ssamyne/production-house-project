import './LoadingSpinner.scss';

const LoadingSpinner = () => {
  return (
    <div id='spinner' className='spinner__container'>
      <div className='spinner__item'></div>
    </div>
  );
};

export default LoadingSpinner;
